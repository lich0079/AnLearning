package algorithm.tree;


public class as {
public static void main(String[] args) {
    String[] indexs = " ".split(",");
    System.out.println(indexs.length);
    for (int i = 0; i < indexs.length; i++) {
        int index = Integer.valueOf(indexs[i]);
        System.out.println(index);
    }
}
}
